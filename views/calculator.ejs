<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
    <link
      href="http://cdn.remixicon.com/releases/v1.1.1/remixicon.css"
      rel="stylesheet"
    />
    <!-- Option B: Latest known version from jsDelivr -->
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.6.0/fonts/remixicon.css"
      rel="stylesheet"
    />
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/calculator.css">

</head>
<body class="p-6 bg-gradient-to-b from-green-100 via-white to-green-100 min-h-screen">
    <a href="/" class="px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700"><i class="ri-arrow-left-line mr-2"></i>back to home</a>

    <div class="mt-7 flex flex-col justify-center items-center gap-2 ">
        <div class="flex justify-center items-center gap-1">
            <div class="w-[50px] h-[50px] bg-green-500 rounded-full flex justify-center items-center text-white text-2xl ">
                <i class="ri-calculator-line"></i>
            </div>
        </div>
        <h1 class="text-3xl font-bold">Carbon Footprint Calculator</h1>
        <p class="text-gray-600 text-center w-[650px]">Calculate your monthly carbon emissions and discover how to reduce your environmental impact</p>
        
    </div>

    <div id="Claculator-part" class="mt-10 flex justify-center items-center gap-10">
        <div class="bg-white p-6 rounded-xl shadow-md w-full max-w-md border border-green-200">
    
    <!-- Title -->
    <div class="flex items-center space-x-2 mb-6">
      <span class="text-green-600 text-xl">🏠</span>
      <h2 class="text-xl font-bold text-gray-800">Environmental Data</h2>
    </div>

    <form id="carbonForm" class="space-y-4">

  <!-- Monthly Electricity Usage -->
  <div>
    <label class="flex items-center space-x-2 font-medium text-gray-700">
      <span class="text-green-600">⚡</span>
      <span>Monthly Electricity Usage (kWh)</span>
    </label>
    <input id="electricity" type="number" placeholder="e.g., 250" 
      class="mt-1 w-full border border-gray-300 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
  </div>

  <!-- Monthly Gas Usage -->
  <div>
    <label class="flex items-center space-x-2 font-medium text-gray-700">
      <span class="text-green-600">🏠</span>
      <span>Monthly Gas Usage (m³)</span>
    </label>
    <input id="gas" type="number" placeholder="e.g., 50" 
      class="mt-1 w-full border border-gray-300 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
  </div>

  <!-- Monthly Water Usage -->
  <div>
    <label class="block font-medium text-gray-700">Monthly Water Usage (L)</label>
    <input id="water" type="number" placeholder="e.g., 3000" 
      class="mt-1 w-full border border-gray-300 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
  </div>

  <!-- Monthly Transport Distance -->
  <div>
    <label class="flex items-center space-x-2 font-medium text-gray-700">
      <span class="text-green-600">🔗</span>
      <span>Monthly Transport Distance (km)</span>
    </label>
    <div class="flex space-x-2 mt-1">
      <input id="transport" type="number" placeholder="e.g., 800" 
        class="w-2/3 border border-gray-300 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
      <select id="transportMode" 
        class="w-1/3 border border-gray-300 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
        <option>Car</option>
        <option>Bus</option>
        <option>Train</option>
        <option>Bike</option>
      </select>
    </div>
  </div>

  <!-- Monthly Waste Generation -->
  <div>
    <label class="block font-medium text-gray-700">Monthly Waste Generation (kg)</label>
    <input id="waste" type="number" placeholder="e.g., 30" 
      class="mt-1 w-full border border-gray-300 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
  </div>

  <!-- Button -->
  <button type="submit" 
    class="w-full bg-green-500 hover:bg-green-600 text-white py-2 rounded-lg font-medium mt-4">
    Calculate Carbon Footprint
  </button>
</form>

<!-- Result Section -->
<div class="mt-6 p-4 border rounded-lg bg-gray-50">
  <h3 class="font-bold text-lg">Result:</h3>
  <p id="result" class="text-green-700 mt-2"></p>
  <ul id="suggestions" class="list-disc list-inside text-gray-700 mt-2"></ul>
</div>

        <!-- </div> -->

        <!-- Another two divs of the calculator part 
        <div class="flex flex-col flex-wrap gap-6 justify-center items-center">

    
    <! <div class="bg-white rounded-xl shadow-md p-6 w-full sm:w-[350px]"> -->
      <!-- Top section -->
        <!-- <div class="text-center  pb-4"> -->
            <!-- <p class="text-green-700 font-medium">Your Carbon Footprint</p>
            <p class="text-5xl font-bold text-gray-800 mt-2">608.5</p>
            <p class="text-gray-500 text-sm">kg CO₂ equivalent per month</p>
            <div class="mt-4 bg-green-50 rounded-lg p-3 flex justify-center items-center gap-2">
              <span class="text-green-600 text-xl">🌳</span>
              <p class="text-green-700 font-medium">28 trees</p>
            </div>
            <p class="text-gray-500 text-xs mt-1">needed to offset your emissions</p> -->


        <!-- </div> -->
      
    <!-- </div> -->

    <!-- card 2.  -->
    <!-- <div class=" mt-5 bg-white rounded-xl shadow-md p-6 w-full sm:w-[350px]"> -->
        <!-- <div class="  ">
        <p class="font-medium text-gray-800 mb-3">Quick Tips to Reduce Emissions</p>
        <ul class="space-y-3 text-sm">
          <li class="flex items-start gap-2">
            <span class="text-green-500 mt-1">●</span>
            <div>
              <p class="text-gray-800 font-medium">Switch to LED lighting</p>
              <p class="text-gray-500 text-xs">Reduce electricity by up to 75%</p>
            </div>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-blue-500 mt-1">●</span>
            <div>
              <p class="text-gray-800 font-medium">Use public transport</p>
              <p class="text-gray-500 text-xs">Cut transport emissions by 60%</p>
            </div>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-green-500 mt-1">●</span>
            <div>
              <p class="text-gray-800 font-medium">Recycle and compost</p>
              <p class="text-gray-500 text-xs">Reduce waste emissions by 40%</p>
            </div>
          </li>
        </ul>
        </div> -->


    <!-- </div> -->

    
    

  </div>
    </div>


    

    <script>
document.getElementById("carbonForm").addEventListener("submit", async function (e) {
  e.preventDefault();

  const formData = {
    electricity: parseFloat(document.getElementById("electricity").value) || 0,
    gas: parseFloat(document.getElementById("gas").value) || 0,
    water: parseFloat(document.getElementById("water").value) / 1000 || 0, // convert Liters → m³
    waste: parseFloat(document.getElementById("waste").value) || 0,
    transport: parseFloat(document.getElementById("transport").value) || 0,
    transportMode: document.getElementById("transportMode").value
  };

  const res = await fetch("http://localhost:3000/calculate-footprint", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(formData)
  });

  const data = await res.json();

  document.getElementById("result").innerText = 
    `Total Carbon Footprint: ${data.totalCarbon} kg CO₂`;

  const suggestionList = document.getElementById("suggestions");
  suggestionList.innerHTML = "";
  data.suggestions.forEach(s => {
    const li = document.createElement("li");
    li.textContent = s;
    suggestionList.appendChild(li);
  });
});
</script>

</body>
</html>